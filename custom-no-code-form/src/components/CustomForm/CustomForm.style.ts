import { CSSProperties } from 'react';

export const CustomFormStyle: CSSProperties = {
    minWidth: '100%',
    minHeight: '100%',
    width: '100%',
    height: '100%',
    display: 'flex',
    flexDirection: 'column',
    gap: '8px',
}
